import models
from database import SessionLocal

"""
def select_oper_game_master(b4_game, session):
    res = session.query(
        models.OperGamesMaster
    ).filter(
        models.OperGamesMaster.b4_code == b4_game  # @UndefinedVariable
    ).first()
    if res:
        return res
"""
